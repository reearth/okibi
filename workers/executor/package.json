{
  "name": "@reearth/okibi-executor",
  "version": "0.0.0",
  "private": true,
  "description": "Drains a warm plan from a queue, for plans too long to run inside a CI job",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260823.1",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
